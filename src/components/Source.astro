---
const { citation } = Astro.props;
---

<html>
  <head>
    <style>
      /* Tooltip Styles */
      .tooltip-container {
        /* no longer position: relative, so the tooltip can be centered on screen */
        display: inline-block;
        white-space: nowrap;
      }

      .source-popup {
        cursor: pointer;
        text-decoration: underline;
      }

      .tooltip {
        /* Use fixed positioning so it's always centered on the viewport */
        position: fixed;
        left: 50%;
        transform: translateX(-50%);
        width: 80vw;
        background: #333;
        color: #fff;
        padding: 8px 16px;
        border-radius: 4px;
        opacity: 0;
        pointer-events: none;
        transition: opacity 0.2s ease-in-out;
        word-break: break-word;
        white-space: normal;
        z-index: 10;
      }

      /* Show the tooltip on hover */
      .source-popup:hover + .tooltip,
      .tooltip:hover {
        opacity: 1;
        pointer-events: auto;
      }
    </style>
  </head>
  <body>
    <div class="tooltip-container">
      <span class="source-popup">SOURCE</span>
      <span class="tooltip">
        <div class="center">
          {citation}
        </div>
      </span>
    </div>
  </body>
</html>
